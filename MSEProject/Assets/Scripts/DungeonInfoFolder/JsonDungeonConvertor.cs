using UnityEngine;

namespace DungeonInfoFolder
{
    public class JsonDungeonConvertor : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
